{
  "name": "rcar",
  "version": "0.0.0",
  "private": true,
  "description": "RCar — Sistema de gestão para locadora de veículos e lavajato",
  "scripts": {
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "dev": "pnpm --filter web dev & pnpm --filter api start:dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}

